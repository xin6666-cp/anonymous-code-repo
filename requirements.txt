# Core (install a torch build matching your CUDA: https://pytorch.org)
torch>=2.1
pytorch-lightning>=2.1
transformers>=4.35
tokenizers>=0.15
torchmetrics>=1.2

# Numerics / data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
Pillow>=10.0
opencv-python>=4.8

# NLG / evaluation metrics
nltk>=3.8
pycocoevalcap>=1.2

# Utilities
pyyaml>=6.0
tqdm>=4.66
tensorboard>=2.14

# Note: RadGraph / RaTEScore reporting (if used) require their own packages;
# install them separately following their official instructions.
